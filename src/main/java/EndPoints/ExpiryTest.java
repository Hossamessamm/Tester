package EndPoints;

import io.restassured.response.Response;
import utils.HttpUtils;

public class ExpiryTest {
    public void ValidchangeExpiry() {
        Response response = HttpUtils.postRequest()
    }
}
